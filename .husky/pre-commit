yarn

forge build
if [ $? -eq 0 ]; then
    echo 'Build successful'
else
    echo 'Please run "forge build" and commit again'
fi

yarn lint:check
if [ $? -eq 0 ]; then
    echo 'All checks passed'
else
    echo 'Please run "yarn lint" and commit again'
fi
